[Unit]
Description=Clipboard history daemon
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/sh -c '/usr/bin/wl-paste --type text --watch /usr/bin/cliphist store'
Restart=always
RestartSec=1

[Install]
WantedBy=graphical-session.target
